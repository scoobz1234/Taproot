class Resident {
    constructor(
        id,
        name,
        facility
    ) {
        this.id = id;
        this.name = name;
        this.facility = facility;
    }
}

export default Resident;