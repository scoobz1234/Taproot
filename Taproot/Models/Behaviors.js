class Behaviors {
    constructor(
        id,
        name,
        interventionids

    ) {
        this.id = id;
        this.name = name;
        this.interventionids = interventionids;
    }
}

export default Behaviors;